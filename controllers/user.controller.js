const UserModel = require('../models/user');

class UsersController {
    getUsers(req, res) { };
    createUser(req, res) { };
    updateUser(req, res) { };
    deleteUser(req, res) { };
}

module.exports = new UsersController();
